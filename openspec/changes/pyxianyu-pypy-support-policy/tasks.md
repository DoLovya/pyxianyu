## 1. OpenSpec 变更

- [x] 1.1 新建 `pyxianyu-pypy-support-policy` 变更并补齐 proposal / design / tasks / spec

## 2. CI 与元数据

- [x] 2.1 在 `.github/workflows/ci.yml` 增加独立的 PyPy smoke job
- [x] 2.2 在 `pyproject.toml` 增加 Python / Implementation classifiers

## 3. 对外支持声明

- [x] 3.1 在 `README.md` 增加兼容性与支持声明
- [x] 3.2 明确 PyPy 的验证边界与支持级别

## 4. 验证

- [x] 4.1 运行本地构建与单测，确认改动未破坏现有打包流程
